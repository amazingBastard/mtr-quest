App.Loading = React.createClass({
    render() {
        return (
            <module className="top fixed centered loading module">
                <p>loading...</p>
            </module>
        );
    }
});