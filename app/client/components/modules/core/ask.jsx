App.Ask = React.createClass({
    render() {
        return (
            <module className="ask question module">
                <input type="text" className="question input" />
            </module>
        );
    }
});